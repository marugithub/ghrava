# LifeTracker — Secrets Template
# ═══════════════════════════════════════════════════════════════
# COPY THIS FILE TO .env.secrets AND FILL IN YOUR VALUES.
# .env.secrets is NEVER included in any patch — it stays on your
# QNAP forever, untouched.
#
# One-time setup:
#   1. Copy:   .env.secrets.template  →  .env.secrets
#   2. Fill in your values below
#   3. Move your existing APP_PASSWORD_HASH from .env into here
# ═══════════════════════════════════════════════════════════════

# ── Auth ────────────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change_this_to_a_long_random_string

# bcrypt hash of your app password — move here from old .env
APP_PASSWORD_HASH=

# ── Google APIs (manual PDF finder — deferred, add when ready) ──
GOOGLE_API_KEY=A 
GOOGLE_CSE_ID= 

# ── Brave Search API (future replacement for Google CSE) ────────
# Free tier: 2000 searches/month — https://api.search.brave.com
BRAVE_SEARCH_KEY=

# ── Pushover Notifications (future) ─────────────────────────────
# $5 one-time app — https://pushover.net
PUSHOVER_TOKEN=
PUSHOVER_USER_KEY=

# ── Finance Integrations (future) ───────────────────────────────
ALPHA_VANTAGE_KEY=
PLAID_CLIENT_ID=
PLAID_SECRET=

# ── Email / SMTP (future monthly summary) ───────────────────────
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# ── Backup Encryption (future) ──────────────────────────────────
BACKUP_ENCRYPTION_KEY=
